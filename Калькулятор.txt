import math

print('''Привет! Я самый лучший и дружелюбный калькулятор на Python!

Я могу посчитать всё, что угодно (кроме очень больших чисел), и сколько угодно раз. 

Что полезно знать при работе со мной:

Я принимаю только числа, скобки (для изменения порядка операций) и верные символы операций:

+ (сложение)
- (вычитание)
* (умножение)
/ (деление)
** (возведение в степень)
% (остаток от деления)
// (целочисленное деление)

Если Вам нужно посчитать квадратный корень, введите math.sqrt() - в скобках укажите число, из которого считаем корень.

В числах с дробями для отделения дробной части используйте точку (.), а не запятую (,).

Для выхода из программы введите букву 'g' и нажмите 'Enter'.

Начнём? Введите арифметический пример после слова 'Считаем:' и нажмите Enter. Знак '=' вводить не нужно.''')
print()

while True:
    s = input('Считаем: ')
    print()
    if s.lower() == "g":
        print('Программа завершена. Надеюсь, что был Вам полезен!')
        break

    try:
        x = eval(s)

        if isinstance(x, (int, float)):
            print(x)
        else:
            print('Результат не является числом. Вероятно, Вы ввели не арифметический пример или использовали кавычки.')

    except (SyntaxError, TypeError, NameError):
        print('Не вышло... Видимо, Вы ввели некорректные данные или символы. Проверьте, пожалуйста, и повторите ввод.')

    except ZeroDivisionError:
        print('Один известный советский математик сказал: "Делить на 0 = это безнравственно".')

    except ValueError:
        print('Получилось такое большое число, что я не могу его вывести. Простите :(')
    print()
